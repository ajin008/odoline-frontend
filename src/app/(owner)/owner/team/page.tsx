import { TeamShell } from "@/src/features/team/components/team-shell";

export const metadata = {
  title: "Team Management | Owner Terminal",
  description: "Manage dealership sales staff profiles, credentials, and department assignments.",
};

export default function TeamPage() {
  return <TeamShell />;
}
